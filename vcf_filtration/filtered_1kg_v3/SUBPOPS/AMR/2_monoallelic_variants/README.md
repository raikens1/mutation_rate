monoallelic_variants
#######################
This directory contains all of the monoallelic variants from AMR_multiallelic
These variants were filtered using awk and the script used to generate these files is found in ../awk_AMR.sh

These variants still need to be filtered for privacy to AMR.
